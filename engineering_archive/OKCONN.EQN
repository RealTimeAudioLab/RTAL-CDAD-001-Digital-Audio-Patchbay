chip okconn gal20v8

  insel recsel pco pck dspo dspk auxo auxk a b c gnd
  d nc da1 da2 rec1 rec2 pc dsp aux fc fci vcc

@ues okconn

equations

 da1   = insel * /a * /b * /c * /d
       + pco   *  a * /b * /c * /d
       + pck   * /a *  b * /c * /d
       + dspo  *  a *  b * /c * /d
       + fci   *  a *  b *  c * /d
       + auxo  *  a * /b *  c * /d
       + auxk  * /a *  b *  c * /d
       + insel *  d

 da2   = insel * /a * /b * /c * /d
       + pco   *  a * /b * /c * /d
       + pck   * /a *  b * /c * /d
       + dspo  *  a *  b * /c * /d
       + fci   *  a *  b *  c * /d
       + auxo  *  a * /b *  c * /d
       + auxk  * /a *  b *  c * /d
       + insel *  d

 rec1  = recsel * /d
       + pco   * /a * /b * /c *  d
       + pck   *  a * /b * /c *  d
       + dspo  * /a *  b * /c *  d
       + dspk  *  a *  b * /c *  d
       + auxo  * /a * /b *  c *  d
       + auxk  *  a * /b *  c *  d
       + fci   * /a *  b *  c *  d

 rec2  = recsel * /d
       + pco   * /a * /b * /c *  d
       + pck   *  a * /b * /c *  d
       + dspo  * /a *  b * /c *  d
       + dspk  *  a *  b * /c *  d
       + auxo  * /a * /b *  c *  d
       + auxk  *  a * /b *  c *  d
       + fci   * /a *  b *  c *  d

 pc    = recsel * /d
       + pco   * /a * /b * /c *  d
       + pck   *  a * /b * /c *  d
       + dspo  * /a *  b * /c *  d
       + dspk  *  a *  b * /c *  d
       + auxo  * /a * /b *  c *  d
       + auxk  *  a * /b *  c *  d
       + fci   * /a *  b *  c *  d
 
 dsp   = insel  * /d
       + recsel *  d

 aux   = insel  * /d
       + recsel *  d

 fc    = insel  * /d
       + recsel *  d



